## Streaming Gemini Response Demo

Make sure you have a GCP project with Vertext API enabled.

Create a .env file at the root with appropriate variables:
PROJECT_ID=<your-gcp-project-id>
LOCATION_ID=<your-project-location-eg-us-central1>

```
npm install
npm start
```

App runs by default on http://localhost:3000